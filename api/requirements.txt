# Bez zakresów wersji — powtarzalne buildy (Vercel, CI, lokalnie).
# Aktualizacja zależności: świadomie, po testach (pytest, npm test).
# Starlette: jawny pin >=0.49.1 (CVE w łańcuchu FastAPI); PyJWT: CVE-2026-32597.

fastapi==0.135.2
starlette==0.49.1
uvicorn==0.34.2
supabase==2.15.2
python-dotenv==1.1.0
email-validator==2.3.0
PyJWT==2.12.1

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
httpx==0.28.1
locust==2.32.10

# Strefy czasowe (Windows: zoneinfo wymaga pakietu tzdata)
tzdata==2025.3

# Generowanie PDF (powiadomienia email o saldzie)
reportlab==4.4.10

# Import Excel (stan początkowy) + upload plików (multipart/form-data)
openpyxl==3.1.5
python-multipart==0.0.22

# Import zestawień bankowych (.xls — stary format Excel)
xlrd==2.0.2
# Zapis testowych plików .xls w pytest (api/tests/test_bank_statement_parser.py)
xlwt==1.3.0
